{
  "name": "@dignetwork/deploy-action",
  "version": "0.1.0",
  "private": true,
  "description": "Official GitHub Action to deploy a build to the DIG Network (push-to-deploy, preview-per-PR).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/DIG-Network/deploy-action.git"
  }
}
